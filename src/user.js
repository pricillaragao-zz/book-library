class User {
  /**
   *
   * @param {String} id
   * @param {String} name
   * @param {String} address
   */
  constructor(id, name, address) {
    this.id = id;
    this.name = name;
    this.address = address;
  }
}

module.exports = User;
